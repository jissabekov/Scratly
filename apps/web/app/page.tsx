const views=[['Profile','Eight dimensions with confidence and status'],['Evidence ledger','Accepted and rejected claims with exact quotes'],['Conversation','Raw student and assistant messages'],['Timeline','Versioned profile transitions'],['Contradictions','Open conflicts and explicit resolutions'],['Question history','Targets, rationale, and fallback use'],['Why this question?','Deterministic priority trace'],['Project ranking','Eligibility, weighted fit, and scaffolding']];
export default function Home(){return <main><header><p className="eyebrow">SCRATLY · TEACHER INSPECTION</p><h1>Student assessment console</h1><p>Every conclusion remains traceable to the student’s own words.</p></header><nav>{views.map(([a])=><a key={a} href={'#'+a.replaceAll(' ','-')}>{a}</a>)}</nav><section className="grid">{views.map(([a,b],i)=><article id={a.replaceAll(' ','-')} key={a}><span>{String(i+1).padStart(2,'0')}</span><h2>{a}</h2><p>{b}</p><div className="empty">Select a student session to inspect</div></article>)}</section></main>}
